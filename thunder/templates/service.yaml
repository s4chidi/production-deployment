apiVersion: v1
kind: Service
metadata:
  name: {{ include "kanibal.fullname" . }}
  labels:
    {{- include "kanibal.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kanibal.selectorLabels" . | nindent 4 }}
